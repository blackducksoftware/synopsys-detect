###
### generated by script
###

language: java
sudo: false
install: true

git:
  depth: false

jdk:
- openjdk8

notifications:
  email:
    recipients:
      - starlabs@synopsys.com

script:
  - "./gradlew clean build testBattery --info"

after_success:
  - bash <(curl -s https://copilot.blackducksoftware.com/ci/travis/scripts/upload)

cache:
  directories:
    - "$HOME/.m2/repository"
    - "$HOME/.gradle"
    - ".gradle"
