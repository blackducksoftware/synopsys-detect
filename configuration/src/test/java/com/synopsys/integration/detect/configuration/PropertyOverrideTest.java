package com.synopsys.integration.detect.configuration;

import java.util.ArrayList;
import java.util.List;

import org.junit.jupiter.api.Assertions;
import org.junit.jupiter.api.Test;

public class PropertyOverrideTest {
    @Test
    public void CheckOverrideContainsNoDuplicateValues() {
//        final List<DetectProperty> found = new ArrayList<>();
//        DetectPropertyDeprecations.PROPERTY_OVERRIDES.forEach((key, value) -> {
//            Assertions.assertFalse(found.contains(value), "Each detect property can only exist in the property override value side exactly once. This was found twice: " + value.getPropertyKey());
//            found.add(value);
//        });
    }
}
