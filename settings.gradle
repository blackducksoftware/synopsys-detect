include "hub-detect"
include "detect-configuration"
include "detect-doctor"

rootProject.name = 'detect'
rootProject.children.each { 
    project -> project.buildFileName = "${project.name}.gradle"
}
