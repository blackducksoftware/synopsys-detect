pluginManagement {
    repositories {
        maven { url 'https://repo.spring.io/release' }
        gradlePluginPortal()
    }
}

rootProject.name = 'synopsys-detect'

include 'detectable'
include 'detector'
include 'configuration'
include 'common'
include 'common-test'